%h1 Admin - About

%nav
  = link_to 'Edit', edit_admin_about_url

%h3  Cover Image
- if @about.cover_image.attachment.present?
  .cover-img= image_tag @about.cover_image.attachment.url

%h3 Text
.description= render_markdown @about.description
